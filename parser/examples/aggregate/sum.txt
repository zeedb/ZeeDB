select sum(1) 
from person

LogicalMap $aggregate.$agg1
	LogicalAggregate $aggregate.$agg1:(Sum $sum)
		LogicalMap $sum:1
			Filter* (LessOrEqual $xmin (Xid)) (Less (Xid) $xmax)
				Map* $xmin $xmax
					LogicalGet person