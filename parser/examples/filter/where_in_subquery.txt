select 1 
from person 
where person_id in (select person_id from customer)

LogicalMap $query.$col1:1
	LogicalFilter $in
		LogicalJoin Mark $in (Equal person.person_id customer.person_id)
			LogicalMap customer.person_id
				Filter* (LessOrEqual $xmin (Xid)) (Less (Xid) $xmax)
					Map* customer.person_id $xmin $xmax
						LogicalGet customer
			Filter* (LessOrEqual $xmin (Xid)) (Less (Xid) $xmax)
				Map* person.person_id $xmin $xmax
					LogicalGet person