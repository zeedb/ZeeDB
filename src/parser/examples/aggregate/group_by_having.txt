select store_id from customer group by 1 having count(*) > 1

LogicalFilter (Greater $aggregate.$agg1 1)
	LogicalAggregate $groupby.store_id $aggregate.$agg1:(CountStar)
		LogicalProject $groupby.store_id:customer.store_id
			LogicalGet customer