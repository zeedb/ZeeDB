select 1 
from person 
where exists (select person_id from customer where customer.person_id = person.person_id)

LogicalMap $query.$col1:1
	LogicalFilter $mark.$exists
		LogicalJoin Mark $mark.$exists (Equal person.person_id person.person_id')
			LogicalDependentJoin [person.person_id']
				LogicalMap customer.person_id
					LogicalFilter (Equal customer.person_id person.person_id')
						Filter* (LessOrEqual customer.$xmin (Xid)) (Less (Xid) customer.$xmax)
							Map* customer.person_id customer.$xmin customer.$xmax
								LogicalGet customer
				Filter* (LessOrEqual person.$xmin (Xid)) (Less (Xid) person.$xmax)
					Map* person.person_id' person.$xmin person.$xmax
						LogicalGet person
			Filter* (LessOrEqual person.$xmin (Xid)) (Less (Xid) person.$xmax)
				Map* person.person_id person.$xmin person.$xmax
					LogicalGet person