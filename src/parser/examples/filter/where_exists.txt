select 1 
from person 
where exists (select person_id from customer where customer.person_id = person.person_id)

LogicalProject $query.$col1:1
	LogicalFilter $mark.$exists
		LogicalJoin Mark $mark.$exists
			LogicalProject customer.person_id
				LogicalFilter (Equal customer.person_id person.person_id)
					Project* customer.person_id
						LogicalGet customer
			Project* person.person_id
				LogicalGet person