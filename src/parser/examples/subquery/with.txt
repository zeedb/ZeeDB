with foo as (select customer_id from customer) select customer_id 
from foo

LogicalWith foo
	LogicalProject customer.customer_id
		Project* customer.customer_id
			LogicalGet customer
	LogicalProject foo.customer_id
		LogicalGetWith foo