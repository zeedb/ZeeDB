select customer_id as a, "foo" as b 
from customer

LogicalMap $query.b:"foo" customer.customer_id
	Filter* (LessOrEqual $xmin (Xid)) (Less (Xid) $xmax)
		Map* customer.customer_id $xmin $xmax
			LogicalGet customer