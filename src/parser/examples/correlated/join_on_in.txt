select 1 
from person 
join store on person.person_id in (select person_id from customer where customer.store_id = store.store_id)

LogicalMap $query.$col1:1
	LogicalFilter $mark.$in
		LogicalJoin Mark $mark.$in (Equal store.store_id store.store_id') (Equal person.person_id customer.person_id)
			LogicalDependentJoin [store.store_id']
				LogicalMap customer.person_id
					LogicalFilter (Equal customer.store_id store.store_id')
						Filter* (LessOrEqual customer.$xmin (Xid)) (Less (Xid) customer.$xmax)
							Map* customer.person_id customer.store_id customer.$xmin customer.$xmax
								LogicalGet customer
				LogicalJoin Inner
					Filter* (LessOrEqual person.$xmin (Xid)) (Less (Xid) person.$xmax)
						Map* person.person_id person.$xmin person.$xmax
							LogicalGet person
					Filter* (LessOrEqual store.$xmin (Xid)) (Less (Xid) store.$xmax)
						Map* store.store_id' store.$xmin store.$xmax
							LogicalGet store
			LogicalJoin Inner
				Filter* (LessOrEqual person.$xmin (Xid)) (Less (Xid) person.$xmax)
					Map* person.person_id person.$xmin person.$xmax
						LogicalGet person
				Filter* (LessOrEqual store.$xmin (Xid)) (Less (Xid) store.$xmax)
					Map* store.store_id store.$xmin store.$xmax
						LogicalGet store