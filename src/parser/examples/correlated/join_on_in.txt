select 1 
from person 
join store on person.person_id in (select person_id from customer where customer.store_id = store.store_id)

LogicalMap $query.$col1:1
	LogicalFilter $in
		LogicalJoin Mark $in (Equal store.store_id store_id) (Equal person.person_id customer.person_id)
			LogicalDependentJoin [store_id]
				LogicalMap customer.person_id
					LogicalFilter (Equal customer.store_id store_id)
						Filter* (LessOrEqual $xmin (Xid)) (Less (Xid) $xmax)
							Map* customer.person_id customer.store_id $xmin $xmax
								LogicalGet customer
				LogicalJoin Inner
					Filter* (LessOrEqual $xmin (Xid)) (Less (Xid) $xmax)
						Map* person.person_id $xmin $xmax
							LogicalGet person
					Filter* (LessOrEqual $xmin (Xid)) (Less (Xid) $xmax)
						Map* store_id $xmin $xmax
							LogicalGet store
			LogicalJoin Inner
				Filter* (LessOrEqual $xmin (Xid)) (Less (Xid) $xmax)
					Map* person.person_id $xmin $xmax
						LogicalGet person
				Filter* (LessOrEqual $xmin (Xid)) (Less (Xid) $xmax)
					Map* store.store_id $xmin $xmax
						LogicalGet store