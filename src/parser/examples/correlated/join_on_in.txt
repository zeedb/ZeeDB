select 1 
from person 
join store on person.person_id in (select person_id from customer where customer.store_id = store.store_id)

LogicalProject $query.$col1:1
	LogicalJoin Inner $mark.$in
		Project* person.person_id
			LogicalGet person
		Project* store.store_id
			LogicalGet store