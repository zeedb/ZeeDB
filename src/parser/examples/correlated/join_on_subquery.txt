select 1 
from person 
join customer on (select person.person_id = customer.person_id)

LogicalMap $query.$col1:1
	LogicalFilter $expr_subquery.$col1
		LogicalJoin Single (Equal person.person_id person.person_id') (Equal customer.person_id customer.person_id')
			LogicalDependentJoin [person.person_id' customer.person_id']
				LogicalMap $expr_subquery.$col1:(Equal person.person_id' customer.person_id')
					LogicalSingleGet
				LogicalJoin Inner
					Filter* (LessOrEqual person.$xmin (Xid)) (Less (Xid) person.$xmax)
						Map* person.person_id' person.$xmin person.$xmax
							LogicalGet person
					Filter* (LessOrEqual customer.$xmin (Xid)) (Less (Xid) customer.$xmax)
						Map* customer.person_id' customer.$xmin customer.$xmax
							LogicalGet customer
			LogicalJoin Inner
				Filter* (LessOrEqual person.$xmin (Xid)) (Less (Xid) person.$xmax)
					Map* person.person_id person.$xmin person.$xmax
						LogicalGet person
				Filter* (LessOrEqual customer.$xmin (Xid)) (Less (Xid) customer.$xmax)
					Map* customer.person_id customer.$xmin customer.$xmax
						LogicalGet customer