select 1 from person join customer on (select person.person_id = customer.person_id)

LogicalProject $query.$col1:1
	LogicalJoin Inner $expr_subquery.$col1
		Project* person.person_id
			LogicalGet person
		Project* customer.person_id
			LogicalGet customer