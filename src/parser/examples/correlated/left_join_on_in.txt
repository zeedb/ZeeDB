select 1 from person left join store on person.person_id in (select person_id from customer where customer.store_id = store.store_id)

LogicalProject $query.$col1:1
	LogicalRightJoin $mark.$in
		LogicalGet store
		LogicalGet person