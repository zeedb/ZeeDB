select 1 in (select store_id from store where store.store_id = customer.store_id) as ok from customer

LogicalProject $query.ok:$mark.$in
	LogicalJoin Mark $mark.$in (Equal 1 store.store_id)
		LogicalFilter (Equal store.store_id customer.store_id)
			LogicalGet store
		LogicalGet customer