select store_id, (select count(*)
from customer where customer.store_id = store.store_id)
from store

Map $query.$col2:$aggregate.$agg1 store.store_id
	HashJoin Single (Equal store.store_id store_id')
		Partition* store_id'
			Aggregate store_id' $aggregate.$agg1:(CountStar)
				Map store_id':customer.store_id, ..
					Filter* (LessOrEqual $xmin (Xid)) (Less (Xid) $xmax)
						Map* customer.store_id $xmin $xmax
							SeqScan customer
		Partition* store.store_id
			Filter* (LessOrEqual $xmin (Xid)) (Less (Xid) $xmax)
				Map* store.store_id $xmin $xmax
					SeqScan store