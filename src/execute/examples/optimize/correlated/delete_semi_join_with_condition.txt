delete customer
where person_id in (select person_id from customer where account_number = 0)

Delete customer
	Map $mark.$in:true customer.$pid customer.$tid customer.$xmax customer.$xmin customer.person_id customer.person_id
		NestedLoop Semi (Equal customer.person_id customer.person_id)
			Filter* (LessOrEqual customer.$xmin (Xid)) (Less (Xid) customer.$xmax) (Equal customer.account_number 0)
				Map* customer.person_id customer.account_number customer.$xmin customer.$xmax
					SeqScan customer
			Filter* (LessOrEqual customer.$xmin (Xid)) (Less (Xid) customer.$xmax)
				Map* customer.person_id customer.$xmin customer.$xmax customer.$pid customer.$tid
					SeqScan customer