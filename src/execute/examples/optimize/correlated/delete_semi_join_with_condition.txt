delete customer
where person_id in (select person_id from customer where account_number = 0)

Delete customer
	Map $in:true $tid $xmax $xmin customer.person_id customer.person_id
		NestedLoop Semi (Equal customer.person_id customer.person_id)
			Filter* (LessOrEqual $xmin (Xid)) (Less (Xid) $xmax) (Equal customer.account_number 0)
				Map* customer.person_id customer.account_number $xmin $xmax
					SeqScan customer
			Filter* (LessOrEqual $xmin (Xid)) (Less (Xid) $xmax)
				Map* customer.person_id $xmin $xmax $tid
					SeqScan customer