select (select customer_id)
from customer

Map $query.$col1:$expr_subquery.customer_id
	HashJoin Single (Equal customer.customer_id customer.customer_id')
		Partition* customer.customer_id'
			Map $expr_subquery.customer_id:customer.customer_id' customer.customer_id'
				NestedLoop Inner
					TableFreeScan
					Aggregate customer.customer_id'
						Filter* (LessOrEqual customer.$xmin (Xid)) (Less (Xid) customer.$xmax)
							Map* customer.customer_id' customer.$xmin customer.$xmax
								SeqScan customer
		Partition* customer.customer_id
			Filter* (LessOrEqual customer.$xmin (Xid)) (Less (Xid) customer.$xmax)
				Map* customer.customer_id customer.$xmin customer.$xmax
					SeqScan customer