drop table nested.foo

Script
	Assign catalog_id 1
		TableFreeScan
	Assign catalog_id catalog.catalog_id
		LookupJoin Single
			Filter* (Equal catalog.catalog_name "nested")
				Map* catalog.parent_catalog_id catalog.catalog_id catalog.catalog_name
					IndexScan* catalog(catalog.parent_catalog_id:@catalog_id)
			TableFreeScan
	Assign table_id table.table_id
		LookupJoin Single
			Filter* (Equal table.table_name "foo")
				Map* table.catalog_id table.table_id table.table_name
					IndexScan* table(table.catalog_id:@catalog_id)
			TableFreeScan
	Delete table
		Map* table.table_id
			IndexScan table(table.table_id:@table_id)
	Call drop_table @table_id
		TableFreeScan