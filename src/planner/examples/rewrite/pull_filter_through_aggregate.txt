select store_id, (select count(*) from customer where customer.store_id = store.store_id) from store

LogicalProject $query.$col2:$aggregate.$agg1 store.store_id
	LogicalJoin Inner
		LogicalProject $aggregate.$agg1
			LogicalAggregate $aggregate.$agg1:(CountStar)
				LogicalFilter (Equal customer.store_id store.store_id)
					LogicalGet customer
		LogicalGet store