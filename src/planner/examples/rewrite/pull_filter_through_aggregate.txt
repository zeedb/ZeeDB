select store_id, (select count(*) from customer where customer.store_id = store.store_id) from store

LogicalMap $query.$col2:$aggregate.$agg1 store.store_id
	LogicalJoin Single (NaturalJoin store.store_id)
		LogicalMap $aggregate.$agg1
			LogicalAggregate store.store_id $aggregate.$agg1:(CountStar)
				LogicalJoin Inner (Equal customer.store_id store.store_id)
					Map* customer.store_id
						LogicalGet customer
					LogicalProject store.store_id
						Map* store.store_id
							LogicalGet store
		Map* store.store_id
			LogicalGet store