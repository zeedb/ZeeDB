select 1 from person where exists (select 1 from customer where customer.person_id = person.person_id)

LogicalProject $query.$col1:1
	LogicalJoin Semi
		Project* $expr_subquery.$col1:1
			Filter* (Equal customer.person_id person.person_id)
				LogicalGet customer
		Project* person.person_id
			LogicalGet person