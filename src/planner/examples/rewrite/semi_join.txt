select 1 from person where exists (select 1 from customer where customer.person_id = person.person_id)

LogicalProject $query.$col1:1
	LogicalFilter $mark.$exists (Equal customer.person_id person.person_id)
		LogicalJoin Mark $mark.$exists
			LogicalProject $expr_subquery.$col1:1
				LogicalGet customer
			LogicalGet person