select 1 
from person, customer 
where person.person_id = customer.person_id 
and customer.store_id in (select store_id from store)

LogicalProject $query.$col1:1
	LogicalFilter (Equal person.person_id customer.person_id)
		LogicalJoin Semi (Equal customer.store_id store.store_id)
			Project* store.store_id
				LogicalGet store
			LogicalJoin Inner
				Project* person.person_id
					LogicalGet person
				Project* customer.person_id customer.store_id
					LogicalGet customer

Project $query.$col1:1
	Filter (Equal person.person_id customer.person_id)
		HashJoin Semi store.store_id=customer.store_id
			Project* store.store_id
				SeqScan store
			NestedLoop Inner
				Project* customer.person_id customer.store_id
					SeqScan customer
				Project* person.person_id
					SeqScan person