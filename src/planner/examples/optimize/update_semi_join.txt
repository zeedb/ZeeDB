update customer 
set account_number = 0 
where person_id in (select person_id from person where first_name = "Joe")

LogicalUpdate customer.account_number:$update.$expr
	LogicalProject $update.$expr:0
		LogicalJoin Semi (Equal customer.person_id person.person_id)
			Project* person.person_id
				Filter* (Equal person.first_name Joe)
					LogicalGet person
			Project* customer.person_id customer.account_number
				LogicalGet customer

Update customer.account_number:$update.$expr
	Project $update.$expr:0
		HashJoin Semi person.person_id=customer.person_id
			Project* person.person_id
				Filter* (Equal person.first_name Joe)
					SeqScan person
			Project* customer.person_id customer.account_number
				SeqScan customer