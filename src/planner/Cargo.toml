[package]
name = "planner"
version = "0.1.0"
edition = "2018"
publish = false

[lib]
path = "lib.rs"

[dependencies]
encoding = { path = "../encoding" }
fixtures = { path = "../fixtures" }
node = { path = "../node" }
parser = { path = "../parser" }